export function Analytics() {
  return (
    <div className="p-6">
      Analytics — waiting for the v0 component. Will stay stubbed with mock
      data replaced by a TODO until the collector exposes an aggregate-metrics
      endpoint (not built yet).
    </div>
  )
}
