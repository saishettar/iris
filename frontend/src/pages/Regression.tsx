export function Regression() {
  return (
    <div className="p-6">
      Regression comparison — waiting for the v0 component. Will stay stubbed
      with a TODO: this view depends on the M3 eval layer (prompt/model
      version tagging + scores), which does not exist yet.
    </div>
  )
}
